# Port the server listens on (Railway/Render set this automatically)
PORT=3001

# Allowed frontend origin — set to your deployed Vite URL in production
# e.g. https://your-app.up.railway.app
# Leave unset (or *) to allow all origins in development
CLIENT_URL=*